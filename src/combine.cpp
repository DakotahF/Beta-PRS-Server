#include <string> 
#include <Weights.h>
#include <Ancestry.h> 

using namespace std; 

//send ancestry script from here, trigger if flagged

//send weight consolidation, trigger if flagged

//send total prs calculation

//allow more verbose output if possible   
